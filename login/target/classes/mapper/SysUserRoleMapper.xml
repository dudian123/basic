<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.mapper.SysUserRoleMapper">

    <resultMap type="org.example.domain.entity.SysUserRole" id="SysUserRoleResult">
        <result property="userId"    column="user_id"    />
        <result property="roleId"    column="role_id"    />
    </resultMap>

    <!-- 根据用户ID删除用户角色关联 -->
    <delete id="deleteByUserId" parameterType="Long">
        DELETE FROM sys_user_role WHERE user_id = #{userId}
    </delete>

    <!-- 根据角色ID与用户ID集合删除关联 -->
    <delete id="deleteByRoleIdAndUserIds">
        DELETE FROM sys_user_role
        WHERE role_id = #{roleId}
          AND user_id IN
        <foreach collection="userIds" item="uid" open="(" separator="," close=")">
            #{uid}
        </foreach>
    </delete>

    <!-- 插入单个用户角色关联 -->
    <insert id="insert" parameterType="org.example.domain.entity.SysUserRole">
        INSERT INTO sys_user_role(
            user_id,
            role_id
        ) VALUES (
            #{userId},
            #{roleId}
        )
    </insert>

    <!-- 批量插入用户角色关联 -->
    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO sys_user_role(user_id, role_id) VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.userId}, #{item.roleId})
        </foreach>
    </insert>

    <!-- 根据用户ID查询角色ID列表 -->
    <select id="selectRoleIdsByUserId" parameterType="Long" resultType="Long">
        SELECT role_id FROM sys_user_role WHERE user_id = #{userId}
    </select>

</mapper>